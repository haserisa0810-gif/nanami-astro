# 開発引き継ぎメモ（2026-03-28）

## 今回反映した内容
- 管理者削除機能を追加
- 占い師削除機能を追加
  - ただし `paid / assigned / in_progress` の案件を持つ占い師は削除不可
- 仮パスワード運用を追加
  - 管理者追加時・占い師追加時は `is_temp_password = true`
  - 初回ログイン時はパスワード変更画面へ強制遷移
  - 管理者: `/admin/account`
  - 占い師: `/reader/profile`
- 管理者向けパスワード変更画面を追加
- 管理画面UIを少し整理
  - 削除ボタン追加
  - パスワード状態表示
  - Stripe Connect状態表示
- Stripe Connect の土台を追加
  - `astrologers.stripe_account_id`
  - `stripe_onboarding_completed`
  - `stripe_charges_enabled`
  - `stripe_payouts_enabled`
  - 管理画面から「Stripe連携開始」「状態更新」可能

## まだ未完了のもの
- Connect 連携後の実送金（payout / transfer 実行）
- 注文確定〜納品完了に応じた自動報酬確定フロー
- 削除前の依存データ整理ポリシー
  - 現状は進行中案件がある占い師だけ削除ブロック
  - 履歴保持を厳密にやるなら論理削除に寄せた方が安全

## ログインID / パスワードの現状
### デフォルト初期値
`.env` 未設定時の既定値:
- 管理者: `admin@example.com / admin1234`
- 占い師: `reader@example.com / reader1234`

### 実運用
- 管理側で初期パスワードを発行
- 初回ログイン時に本人が変更
- 管理側で再発行した場合も `is_temp_password = true` に戻る

## 変更した主なファイル
- `models.py`
- `bootstrap_platform.py`
- `auth.py`
- `routes_admin.py`
- `routes_reader.py`
- `templates/admin_astrologers.html`
- `templates/admin_users.html`
- `templates/admin_account.html`
- `templates/reader_profile.html`

## 反映前にやること
- 起動時 bootstrap が走る構成ならそのままでOK
- 既存DBへ新カラム追加のため、アプリ起動時に `bootstrap_platform.init_db()` を通す

## Stripe Connect で必要な環境前提
- `STRIPE_SECRET_KEY`
- `PUBLIC_BASE_URL` もしくは `BASE_URL`

※ 今回は Connect口座の作成・オンボーディングURL発行・状態更新まで。
※ 実際の報酬送金ロジックは別途追加前提。
